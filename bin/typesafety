#!/usr/bin/env bash

# ------------------------------------
# Usage: typesafety [WWW-ROOT]
# ------------------------------------

touch .hhconfig
hh_client restart 2> /dev/null

SCRIPT_DIR=$(cd $(dirname $0) && pwd)

hhvm $SCRIPT_DIR/typesafety.hh $1
